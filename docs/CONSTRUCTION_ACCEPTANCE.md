# 三种构建方式验收记录

公开文档中的 `local-record-*` 是脱敏占位符，真实运行、流程和产物编号仅保留在本机验收记录。

日期：2026-09-19；环境：macOS、本地 Python 3.12、Node 26、Rust 1.93；浏览器操作通过真实 Studio 页面完成。范围是代码、可视低代码，以及按最新反馈改造的自然语言零代码构建。

## 代码

Python SDK/嵌入式 Hub、JavaScript SDK、Rust 类型化 Workflow 均通过真实 HTTP 运行。包含输入暂停→respond→恢复、工具/模型节点、依赖/引用、错误处理、审批，以及三语言插件的真实子进程往返。`test_interop.py` 和 `examples.demos.run_all` 提供可复现入口。

## 可视低代码

浏览器创建并保存「验收 · 节点连线与保存」：拖动卡片、真实鼠标拉线传递字符串、循环连接拒绝、断线、撤销、保存后重新载入，最后运行两个节点并核对它们都收到了修改后的内容。object→string 不兼容连接被拒绝，string→string 连接通过。

「演示 · 等待用户补充日期」通过 input 节点暂停，填写运行面板生成的表单后恢复完成。API 集成检查参数缺失、非法循环在执行前拒绝，布局与预算/初始输入往返保持。

本轮修复：卡片文字尚未失焦时点击运行可能丢失修改；断线后自由字段端口消失；删除执行依赖后遗留引用；搜索节点将未填可选字段送为空字符串；首次加载时连线绘制早于图数据；适应画布需要按节点宽度和高度计算且避免浏览器隐藏滚动位移。

## 旧版表单式零代码（历史验收）

浏览器完成模板选择、修改名称/用途/工具/预算、保存、再次加载和试运行。「验收 · 表单助手与工具」运行 `5366493b…` 已完成，core.echo 工具结果为「表单工具调用验收」。模型使用确定性夹具，验证助手配置→Agent→工具→输出链路，不代表真实模型推理效果。

API 集成检查助手更新、版本记录、工具权限拒绝、导出 ZIP（含 Python/JS/Rust 入口）及预算保留。

这些历史检查只验证了单 Agent 调用链，未证明完整工作流已生成。新零代码不再让用户选择工具、Skill 和策略，也不再以单 Agent 配置代替需求编排。

## 自然语言零代码与模型连接修复

浏览器使用用户已连接的 `gpt6`（模型标识 `gpt-6-astra`），创建「验收 · 通知整理完整流程」。模型实际生成四步：extract_notice（结构化提取）→ serialize_extraction（core.to_text）→ draft_checklist（写行动清单）→ save_checklist（保存文本产物）。页面展示中文步骤、连线、完整 JSON，并可打开到节点画布。

运行 `local-record-09` 使用合成通知，四个节点全部成功，产出「通知行动清单.txt」。明确日期和负责人保留；缺少日期/负责人的纸箱清单、宽带迁移标记待确认。没有真实业务写操作。证据保存在本机 `.eah/acceptance/notice-workflow.json`、`notice-assistant.zip`、`notice-checklist.txt`。

修复内嵌浏览器中临时 Blob 未落盘的问题：本地无认证的项目、工作流 JSON 和产物采用服务端附件下载。浏览器三个下载事件均成功，实际落盘的 `Downloads/my-assistant (1).zip` 为 7387 字节、11 个文件，解压核对含四个完整节点；原有旧包未覆盖。带服务认证时保留请求头鉴权下载。

另用同一真实模型检验图片 OCR 需求，结果明确指出当前没有 OCR/图像输入能力，构建状态为 clarification，没有生成假流程。此项证明缺少能力会被展示，不代表已经实现 OCR。

`test_no_code_builder.py` 通过真实本地模型 HTTP、SQLite、Hub HTTP 和三个导出客户端验证：构建时不执行 API；预览、导出、执行使用同一图；工具选择来自注册表；旧需求失效；缺少 OCR 能力返回澄清且不能导出；隐藏工具调用和错误参数被拒绝；显式写调用等待审批；嵌套流程可导出；没有真实模型时不能伪造构建成功。

模型窗口浏览器验收：测试成功自动关闭、请求进行中顶部关闭、失败后底部关闭、Escape 关闭。失败提示显示服务状态而不是 JSON 解析错误；修正配置后可以复用同一名称重试。连接 API 集成测试也验证失败不注册、成功才注册，错误不会泄漏凭证或供应商正文。

本机更新预览临时使用 `http://127.0.0.1:8766/` 和 `.eah/updated-studio.db`。保留 8765 旧进程承载用户原有模型凭证，新预览通过本机请求复用它；未读取或导出密钥。常规项目启动仍默认 8765。

## 搜索与用户 API

`test_search_and_apis.py` 覆盖专用 TinyFish 适配器、三种构建入口、密钥与配置隔离、fresh registry 配置恢复、过滤规则、429 重试、401/超时/坏 JSON/响应超限/错误 Schema/重定向拒绝；另覆盖只读 POST、路径/查询/头/cookie/表单混合请求、OpenAPI 预览/选择/原子导入与写调用审批。

浏览器通过新的三种接入表单分别注册 `demo.tinyfish`、`demo.manual` 和 `demo.imported.lookup`，都指向本地合成服务。保存「验收 · 搜索与自定义 API 完整流程（合成）」并运行：输入 → TinyFish 协议搜索 → 手动 POST 读取 → 导入的 POST 操作等待确认 → 点击确认 → 收到回执。运行 `845070a0…` 的四个节点全部完成，搜索来源链接为 `https://example.org/synthetic-source`，结果明确标注合成。

另外，浏览器通过表单助手「验收 · 表单助手搜索（本地合成）」调用 demo.tinyfish，运行 `3c145cb4…` 完成并保留来源。画布修复后重新加载和缩放，所有节点完整可见，复查没有新增 console error。

## 通过范围与后续关卡

以上入口在当前本机集成范围通过。完整套件结果和包装安装验证见 [VALIDATION.md](VALIDATION.md)。原始运行保存在本机 `.eah/delivery.db`，示例数据不包含真实生活项目。

未验证：新手无指导任务、Linux/Windows 实际 CI、Docker 实际隔离、72 小时长稳。后续已增加真实 TinyFish、GPT 网关和 Jev 调用；搜索报告流程未通过最终复核，运行时 API/工作流开发已另行通过真实验收，见 [开发能力](RUNTIME_DEVELOPMENT.md)；报告质量结果见 [真实验收](LIVE_WORKFLOW_ACCEPTANCE.md)。图片上传/OCR、生成失败自动修复仍未实现。
